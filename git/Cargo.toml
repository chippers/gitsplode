[package]
name = "git"
version = "0.1.0"
authors = ["chip <chip@chip.sh>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
git-sys = { path = "../git-sys" }
thiserror = "1"
